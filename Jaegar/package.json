{
    "name": "jaegar",
    "private": true,
    "scripts": {
        "build:dev": "wally install && rojo sourcemap dev.project.json --output sourcemap.json && wally-package-types --sourcemap sourcemap.json Packages"
    },
    "dependencies": {
        "@jaeymo/toybox": "^1.2.0"
    }
}
